# Getting Started

## Installation

```bash
bun install spotify-cli
```

## Authentication

The CLI uses Spotify's PKCE auth flow. Run any command and you'll be prompted to authenticate on first use.

## Usage

```bash
spotify <command> [options]
```

Use `spotify --help` to see all available commands.
